name: Deploy Angular App to Nginx

on:
  release:
    types: [published]
  workflow_dispatch:
  push :
    branches : main

jobs:
  deploy:
    runs-on: self-hosted
    name: Deploy Angular Build to Nginx

    steps:
      # 1️⃣ Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Download latest release zip from GitHub Release
      - name: Download latest release asset
        uses: robinraju/release-downloader@v1.11
        with:
          repository: ${{ github.repository }}
          latest: true
          fileName: angular-build.zip
          token: ${{ secrets.GITHUB_TOKEN }}

      # 3️⃣ Unzip the release build
      - name: Extract release zip
        run: |
          echo "📦 Extracting release..."
          mkdir -p /tmp/build
          unzip -o angular-build.zip -d /tmp/build/
          ls -l /tmp/build

      # 4️⃣ Copy config.json based on environment
      - name: Inject environment config
        run: |
          # Set default env
          ENV=${{ github.event.inputs.env || 'dev' }}
          echo "🌍 Selected environment: $ENV"

          CONFIG_SOURCE="./config/config.${ENV}.json"
          CONFIG_TARGET="/tmp/build/assets/config.json"

          if [ -f "$CONFIG_SOURCE" ]; then
            echo "✅ Using config file: $CONFIG_SOURCE"
            cp "$CONFIG_SOURCE" "$CONFIG_TARGET"
          else
            echo "⚠️ Config file for $ENV not found. Using existing one."
          fi

      # 5️⃣ Deploy to environment-specific directory
      - name: Deploy to Nginx
        run: |
          ENV=${{ github.event.inputs.env || 'dev' }}
          DEPLOY_DIR="/var/www/${ENV}"

          echo "📂 Deploying to $DEPLOY_DIR"
          sudo mkdir -p $DEPLOY_DIR
          sudo rm -rf ${DEPLOY_DIR:?}/*
          sudo cp -r /tmp/build/* $DEPLOY_DIR/
          sudo chown -R www-data:www-data $DEPLOY_DIR
          sudo chmod -R 755 $DEPLOY_DIR

      # 6️⃣ Verify deployment success
      - name: Verify Deployment
        run: |
          ENV=${{ github.event.inputs.env || 'dev' }}
          DEPLOY_DIR="/var/www/${ENV}"

          if [ -f "${DEPLOY_DIR}/index.html" ]; then
            echo "✅ index.html found. Angular app deployed successfully!"
          else
            echo "❌ index.html missing. Deployment failed."
            echo "📂 Directory contents:"
            sudo ls -l ${DEPLOY_DIR}
            exit 1
          fi

      # 7️⃣ Reload Nginx to apply any changes
      - name: Reload Nginx
        run: |
          sudo nginx -t && sudo systemctl reload nginx
          echo "🚀 Nginx reloaded successfully!"
